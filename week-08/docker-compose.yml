services:
  mongo:
    image: mongo:7
    container_name: coworking-mongo-security
    ports:
      - "27017:27017"
    volumes:
      - coworking_security_data:/data/db

volumes:
  coworking_security_data:
